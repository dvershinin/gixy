# nginx 1.29.3+ add_header_inherit on is the default replacement behavior.
server {
    add_header X-Frame-Options "SAMEORIGIN";

    location / {
        add_header_inherit on;
        add_header Cache-Control "public";
    }
}
