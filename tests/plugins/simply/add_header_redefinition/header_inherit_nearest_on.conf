# A nearer on overrides an ancestor merge and restores replacement behavior.
server {
    add_header_inherit merge;
    add_header X-Frame-Options "SAMEORIGIN";

    location / {
        add_header_inherit on;
        add_header Cache-Control "public";
    }
}
